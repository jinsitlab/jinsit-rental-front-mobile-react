import type { Order } from "../domain/order/order.model"

export const fetchConstructionOrder = async (encryptedId: string) => {
  // TODO: API 연동
  return null
}

export const submitOrder = async (order: Order) => {
  // TODO: API 연동
  return true
}